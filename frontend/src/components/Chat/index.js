import ChatList from './ChatList';
import ChatContent from './ChatContent';
import ChatInput from './ChatInput';

export { ChatList, ChatContent, ChatInput };

// 或者使用默认导出，根据需要
// export default { ChatList, ChatContent, ChatInput };
